interface:
  display_name: "Onboard"
  short_description: "Bounded, visibility-safe, source-cited repository orientation"
  default_prompt: "Use $onboard to return a read-only onboarding_context/v2 summary that applies root and nested instructions, fixed context/Git budgets, pre-read sensitive-data denial, and optional current canonical stage/help evidence without inventing hierarchy, project facts, or actions."
