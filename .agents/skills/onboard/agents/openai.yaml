interface:
  display_name: "Onboard"
  short_description: "Read-only, bounded, source-cited project orientation"
  default_prompt: "Use $onboard to return a role-aware onboarding summary with bounded sources, visibility controls, provenance, and explicit partial/error states."
