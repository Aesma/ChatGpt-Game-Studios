interface:
  display_name: "Changelog"
  short_description: "Build local changelog entries from exact Git-range evidence"
  default_prompt: "Use $changelog with an explicit from/to range manifest to compute net committed changes, keep version/candidate/deployment states separate, sanitize any player draft, and persist only a history-preserving keyed entry."
