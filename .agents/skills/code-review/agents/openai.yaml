interface:
  display_name: "Code Review"
  short_description: "Read-only, coverage-gated architecture and code-quality review."
  default_prompt: "Use $code-review to review target files without editing and report coverage with the verdict."
