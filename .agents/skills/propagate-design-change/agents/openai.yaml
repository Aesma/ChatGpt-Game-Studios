interface:
  display_name: "Propagate Design Change"
  short_description: "When a GDD is revised, scans all ADRs and the traceability..."
  default_prompt: "Use $propagate-design-change to run the Propagate Design Change game-studio workflow."
