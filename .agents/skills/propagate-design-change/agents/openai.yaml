interface:
  display_name: "Propagate Design Change"
  short_description: "Trace a GDD change into stable owner-routed impacts"
  default_prompt: "Use $propagate-design-change to compare one exact GDD baseline/current pair, publish a bounded immutable impact record, and route downstream work without changing owner artifacts."
