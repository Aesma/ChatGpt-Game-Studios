interface:
  display_name: "Create Architecture"
  short_description: "Draft ADR-derived architecture with independent review"
  default_prompt: "Use $create-architecture to author or resume a DRAFT, ADR-derived master architecture and keep review independent."
