interface:
  display_name: "Asset Audit"
  short_description: "Read-only adapter-backed asset compliance audit"
  default_prompt: "Use $asset-audit with an exact hash-bound audit manifest to verify asset metadata, rules, references, and production eligibility; unsupported coverage returns PARTIAL, never COMPLIANT."
