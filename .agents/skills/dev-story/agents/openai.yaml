interface:
  display_name: "Dev Story"
  short_description: "Implement an authorized, provenance-safe story transaction"
  default_prompt: "Use $dev-story to validate provenance and dependencies, authorize an exact owned fileset, implement the story with tests, and hand it to review."
