interface:
  display_name: "Dev Story"
  short_description: "Implement one READY story with owned writes and test proof"
  default_prompt: "Use $dev-story with one hash-bound request to validate a persisted READY gate, authorize a closed owner/path plan, implement and test it, synchronize tracker-owned lifecycle projections, and stop at In Review."
