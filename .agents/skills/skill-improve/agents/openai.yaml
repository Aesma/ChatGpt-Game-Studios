interface:
  display_name: "Skill Improve"
  short_description: "Stage and verify skill fixes against a frozen oracle"
  default_prompt: "Use $skill-improve to freeze independent acceptance evidence, stage a target-local candidate, verify it without touching the live skill, and CAS-apply only a proven improvement."
