interface:
  display_name: "Team UI"
  short_description: "Implement approved UX with final-build verification"
  default_prompt: "Use $team-ui with an exact UI feature request manifest; production implementation requires a separate bounded manifest."
