interface:
  display_name: "Team UI"
  short_description: "Coordinate UX into independently verified UI"
  default_prompt: "Use $team-ui with one cgs.team-ui-request/v2 manifest. Keep UX, visual, engine, implementation, runtime evidence, and review ownership separate; production completion requires a configured engine and current final-build evidence."
