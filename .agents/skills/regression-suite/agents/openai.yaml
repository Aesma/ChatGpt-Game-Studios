interface:
  display_name: "Regression Suite"
  short_description: "Report or CAS-maintain deterministic regression selection"
  default_prompt: "Use an explicit $regression-suite report, update, or audit subcommand with one exact scope manifest to compute change-impact and coverage-unit selection, preserve tombstones and unknowns, and keep build-bound execution receipts separate from manifest operation status."
