interface:
  display_name: "Regression Suite"
  short_description: "Maintain selection; verify receipts separately"
  default_prompt: "Use $regression-suite to maintain an ID-keyed, hash-bound test selection manifest without executing tests or claiming release results."
