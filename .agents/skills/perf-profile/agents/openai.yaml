interface:
  display_name: "Performance Profile"
  short_description: "Plan captures or analyze hash-bound runtime profiler exports"
  default_prompt: "Use $perf-profile in prepare-capture or analyze-export mode; emit budget verdicts only from complete runtime evidence bound to build, platform, hardware, scenario, budget, and input hashes."
