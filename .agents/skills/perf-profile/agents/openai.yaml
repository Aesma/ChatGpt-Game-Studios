interface:
  display_name: "Performance Profile"
  short_description: "Validate bounded profiler exports against exact platform budgets"
  default_prompt: "Use $perf-profile in one explicit mode to prepare a versioned capture matrix or read-only analyze validated runtime exports; preserve platform/build/scenario/profiler identity, report p50/p95/p99/max and spikes per cell, avoid causal claims, and never persist evidence or modify project files."
