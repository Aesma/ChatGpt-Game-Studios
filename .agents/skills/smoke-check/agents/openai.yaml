interface:
  display_name: "Smoke Check"
  short_description: "Run build-bound smoke checks for QA hand-off"
  default_prompt: "Use $smoke-check with an exact candidate manifest, CURRENT QA plan, and unique run ID."
