interface:
  display_name: "Smoke Check"
  short_description: "Run the critical path smoke test gate before QA hand-off"
  default_prompt: "Use $smoke-check to run the Smoke Check game-studio workflow."
