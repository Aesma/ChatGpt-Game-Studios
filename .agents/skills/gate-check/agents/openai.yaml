interface:
  display_name: "Gate Check"
  short_description: "Read-only, hash-bound phase-transition assessment"
  default_prompt: "Use $gate-check with an exact transition ID to assess readiness without changing project stage."
