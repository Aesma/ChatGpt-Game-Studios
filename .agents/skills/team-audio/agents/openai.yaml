interface:
  display_name: "Team Audio"
  short_description: "Create one bounded reviewed audio spec; never implementation"
  default_prompt: "Use $team-audio with a versioned request manifest to route bounded read-only audio, accessibility, technical, engine, QA-planning, and review evidence into one approved design/audio/audio-<artifact-id>.md specification with one writer; do not write code, tests, ADRs, QA plans, or assets."
