interface:
  display_name: "Team Audio"
  short_description: "Create one reviewed audio spec; no code, tests, or assets"
  default_prompt: "Use $team-audio with one artifact ID to coordinate read-only audio specialists and approve one hash-bound design/audio/audio-[artifact-id].md specification; do not implement code, tests, or assets."
