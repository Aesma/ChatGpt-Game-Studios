interface:
  display_name: "Architecture Decision"
  short_description: "Creates an Architecture Decision Record (ADR) documenting a..."
  default_prompt: "Use $architecture-decision to run the Architecture Decision game-studio workflow."
