interface:
  display_name: "Balance Check"
  short_description: "Read-only typed and reproducible balance analysis"
  default_prompt: "Use $balance-check with an exact typed input manifest to analyze formulas, units, targets, and reproducible scenarios without editing or selecting product values."
