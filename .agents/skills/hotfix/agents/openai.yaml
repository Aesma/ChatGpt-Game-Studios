interface:
  display_name: "Hotfix"
  short_description: "Prepare a hash-bound local hotfix candidate"
  default_prompt: "Use $hotfix with one explicit command to plan, patch, build, assess, or hand off a candidate; never merge, deploy, or publish implicitly."
