interface:
  display_name: "Test Evidence Review"
  short_description: "Review test quality and build-bound evidence"
  default_prompt: "Use $test-evidence-review with an exact evidence-review manifest; add --persist only when a durable review is needed."
