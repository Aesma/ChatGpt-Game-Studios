interface:
  display_name: "Design System"
  short_description: "Author or revise one versioned system GDD with explicit decisions"
  default_prompt: "Use $design-system to author or revise one system-gdd/v2 within bounded context, write only the GDD and its checkpoint, and stop at the independent-review handoff."
