interface:
  display_name: "Bug Triage"
  short_description: "Read-only bug snapshot and disposition proposals"
  default_prompt: "Use $bug-triage to inspect production/qa/bugs/, validate the full open-backlog snapshot, and return evidence-backed proposals without changing bug or sprint records."
