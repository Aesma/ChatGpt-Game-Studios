interface:
  display_name: "Project Stage Detect"
  short_description: "Canonical catalog-backed stage evidence packet"
  default_prompt: "Use $project-stage-detect to validate only the catalog-declared stage authority closure and return one canonical cgs.project-stage-detection/v2 packet with current revisions, deterministic blockers, and no artifact-count inference or persistence."
