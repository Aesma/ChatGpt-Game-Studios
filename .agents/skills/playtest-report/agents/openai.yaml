interface:
  display_name: "Playtest Report"
  short_description: "Create traceable playtest protocols and reports"
  default_prompt: "Use $playtest-report to create a protocol, ingest session evidence, or finalize a build-bound report."
