interface:
  display_name: "Release Checklist"
  short_description: "Normalize candidate-bound release evidence"
  default_prompt: "Use $release-checklist with an exact release-candidate manifest and hash-bound evidence index."
