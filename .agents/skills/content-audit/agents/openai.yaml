interface:
  display_name: "Content Audit"
  short_description: "Verify content IDs against current build evidence"
  default_prompt: "Use $content-audit to compare authoritative content IDs with target build-inclusion evidence and return a read-only, fail-closed audit."
