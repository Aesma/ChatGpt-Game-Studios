interface:
  display_name: "Content Audit"
  short_description: "Verify stable content IDs with build evidence"
  default_prompt: "Use $content-audit to compare authoritative stable content IDs with one exact target build through a bounded, revision-bound manifest, explicit coverage, and fail-closed PARTIAL behavior; return the evidence packet without changing project state."
