interface:
  display_name: "Team Release"
  short_description: "Gate and execute one exact release safely"
  default_prompt: "Use $team-release with one exact v2 request to verify immutable release evidence and perform at most the separately authorized action named by that request; preserve receipts through reconciliation and stabilization."
