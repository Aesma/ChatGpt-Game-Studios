interface:
  display_name: "Security Audit"
  short_description: "Redacted, bounded, evidence-bound security assessment"
  default_prompt: "Use $security-audit for a strictly read-only, threat-scoped assessment with secret-safe output, versioned tool receipts, explicit unsupported/partial coverage, and no ship-approval claim."
