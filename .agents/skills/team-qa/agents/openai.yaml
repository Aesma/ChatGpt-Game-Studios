interface:
  display_name: "Team QA"
  short_description: "Run bounded evidence QA for one exact scope and build"
  default_prompt: "Use $team-qa with one exact v2 request; keep immutable checkpoints and workflow completion separate from the evidence-bound QA verdict and every downstream release authority."
