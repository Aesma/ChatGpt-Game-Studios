interface:
  display_name: "Team QA"
  short_description: "Run evidence-bound QA for one exact build candidate"
  default_prompt: "Use $team-qa with an exact candidate manifest, QA plan, and smoke receipt; keep workflow completion separate from the QA verdict."
