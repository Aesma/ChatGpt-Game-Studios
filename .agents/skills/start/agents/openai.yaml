interface:
  display_name: "Start"
  short_description: "Canonical-packet onboarding and safe catalog routing"
  default_prompt: "Use $start to consume one current canonical stage packet, recommend the first evidence-safe catalog action, and optionally persist versioned non-authoritative onboarding preferences without changing stage, review mode, or running another workflow."
