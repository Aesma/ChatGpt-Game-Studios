interface:
  display_name: "Story Done"
  short_description: "Fail-closed, hash-bound story completion gate"
  default_prompt: "Use $story-done to verify every required acceptance criterion with current automated or build-bound manual evidence before closing the story."
