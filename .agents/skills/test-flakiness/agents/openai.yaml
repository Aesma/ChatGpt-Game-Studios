interface:
  display_name: "Test Flakiness"
  short_description: "Analyze homogeneous run sets; quarantine stays external"
  default_prompt: "Use $test-flakiness to analyze an identity-bound run-set manifest without modifying tests, CI, or the regression selection manifest."
