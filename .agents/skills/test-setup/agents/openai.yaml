interface:
  display_name: "Test Setup"
  short_description: "Scaffold and verify engine tests and CI receipts"
  default_prompt: "Use $test-setup to audit or scaffold the test framework, then verify discovery, pass/fail exit sensitivity, and build-bound CI evidence before calling setup complete."
