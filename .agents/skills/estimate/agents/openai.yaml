interface:
  display_name: "Estimate"
  short_description: "Produce relative or calibrated effort evidence"
  default_prompt: "Use $estimate with an explicit story, sprint, or freeform profile to produce a read-only estimate bound to Scope ID and baseline/current hashes."
