interface:
  display_name: "Localize"
  short_description: "Build owner-separated localization evidence"
  default_prompt: "Use $localize with an explicit subcommand and locale; never treat templates, drafts, static checks, or QA plans as completed localization."
