interface:
  display_name: "Day One Patch"
  short_description: "Plan and evidence-gate one exact day-one patch"
  default_prompt: "Use $day-one-patch with an explicit plan or gate mode and hash-bound manifests; it prepares bounded artifacts and evaluates a new candidate without implementing, deploying, or publishing."
