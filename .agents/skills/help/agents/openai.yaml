interface:
  display_name: "Help"
  short_description: "One receipt-backed action from a canonical stage packet"
  default_prompt: "Use $help to validate one current cgs.project-stage-detection/v2 packet and its bound catalog, distinguish verified receipts from claims or present artifacts, expose all earliest conflicts, and recommend exactly one action without writing or executing it."
