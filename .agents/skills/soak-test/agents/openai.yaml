interface:
  display_name: "Soak Test"
  short_description: "Plan and finalize build-bound endurance runs"
  default_prompt: "Use $soak-test to create an immutable endurance protocol, ingest hashed run evidence, or finalize a build-, workload-, and environment-bound soak result."
