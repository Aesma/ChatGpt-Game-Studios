interface:
  display_name: "Vertical Slice"
  short_description: "Plan or evaluate a bounded evidence-driven slice"
  default_prompt: "Use $vertical-slice in one explicit mode to validate exact prerequisites and scope, freeze finite build/playtest/velocity/network evidence requirements, or independently derive a build-bound verdict; implementation and evidence capture remain separate authorized tasks."
