interface:
  display_name: "Prototype"
  short_description: "Run bounded prototypes with user-owned decisions"
  default_prompt: "Use $prototype to run one isolated, budgeted experiment; prototype writes, evidence publication, and downstream work remain separately authorized."
