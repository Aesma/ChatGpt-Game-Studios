interface:
  display_name: "Consistency Check"
  short_description: "Read-only audit of competing GDD claims"
  default_prompt: "Use $consistency-check to compare GDD claims, formulas, ownership, and dependencies without changing project files."
