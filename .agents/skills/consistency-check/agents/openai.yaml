interface:
  display_name: "Consistency Check"
  short_description: "Read-only, revision-bound audit of competing GDD claims."
  default_prompt: "Use $consistency-check to run a bounded, revision-bound comparison of GDD claims, formulas, ownership, and dependencies without changing project files."
