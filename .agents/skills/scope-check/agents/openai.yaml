interface:
  display_name: "Scope Check"
  short_description: "Compare immutable scope artifacts by stable IDs"
  default_prompt: "Use $scope-check compare with explicit baseline and current manifest paths for a read-only, evidence-bound scope delta report."
