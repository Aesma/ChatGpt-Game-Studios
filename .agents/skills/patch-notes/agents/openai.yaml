interface:
  display_name: "Patch Notes"
  short_description: "Draft from exact production deployment evidence; never publish"
  default_prompt: "Use $patch-notes to create a traceable local draft only when an approved exact candidate has a matching verified production deployment receipt."
