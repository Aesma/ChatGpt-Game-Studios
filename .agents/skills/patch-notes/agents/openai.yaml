interface:
  display_name: "Patch Notes"
  short_description: "Draft local notes only for a verified production candidate"
  default_prompt: "Use $patch-notes with a version-pinned v3 request to validate the exact approved candidate and canonical successful production DEPLOY action receipt, derive net-surviving player claims, enforce security/privacy/embargo/localization receipts, and remain read-only unless one absent draft target has explicit create-only version and existence conflict check authority. Never publish."
