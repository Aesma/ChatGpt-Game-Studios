interface:
  display_name: "Test Helpers"
  short_description: "Generate verified real-SUT test helpers"
  default_prompt: "Use $test-helpers with an exact helper-request manifest containing engine, framework, SUT, and validation bindings."
