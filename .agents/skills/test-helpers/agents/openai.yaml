interface:
  display_name: "Test Helpers"
  short_description: "Generate deterministic helpers bound to real production APIs"
  default_prompt: "Use $test-helpers with an exact v2 request and current test-setup receipt to bind real production APIs, deterministic fixtures, compile/discovery evidence, partial-result ledgers, and ownership-safe version and existence conflict check publication."
